<?xml version="1.0" encoding="utf-8"?>
<paths xmlns:android="http://schemas.android.com/apk/res/android">
    <!--
       External Path is same as Environment.getExternalStorageDirectory()
       so we just tack HP on the end
     \-->
    <external-path name="my_files" path="HP"/>
</paths>